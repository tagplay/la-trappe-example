{
  "name": "la-trappe",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/browserify index.js -t envify -o assets/bundle.js",
    "postinstall": "npm run build",
    "start": "npm run build && node ./init.js"
  },
  "author": "Fannar Snær <fannar@lambagras.is>",
  "license": "MIT",
  "dependencies": {
    "browserify": "^10.2.4",
    "cluster-master": "^0.2.0",
    "envify": "^3.4.0",
    "img": "^1.0.0",
    "st": "^0.5.3",
    "tagplay": "^1.0.0"
  }
}
